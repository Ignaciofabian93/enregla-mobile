export type VehicleBrand = {
  id: number;
  brand_id: number;
  brand: string;
  logo: string;
};

export type VehicleModel = {
  id: number;
  brand_id: number;
  model_id: number;
  model: string;
};
